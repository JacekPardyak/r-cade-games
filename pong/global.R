library(shiny)
library(beepr)
library(sets)
HEIGHT = 300
WIDTH = 300 
SIZE = 30

#RV <- reactiveValues(data = list(x = runif(1) * WIDTH, y = runif(1) * HEIGHT))

#RV <- reactiveValues(data = list(x = 290, y = 250))

